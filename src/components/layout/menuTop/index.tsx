import React from "react";
// import { Container } from "./styles";

interface IMenuTop {}

const MenuTop: React.FC<IMenuTop> = (props) => {
  const {} = props;

  return <Container></Container>;
};

export default MenuTop;
