import React from "react";
import { Container } from "./styles";

interface IHome {}

const Home: React.FC<IHome> = (props) => {
  const {} = props;

  return <Container>PAGINA INICIAL HOME</Container>;
};

export default Home;
